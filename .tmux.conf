#==============================================================#
#                .tmux.conf                                    #
#==============================================================#

source-file "$HOME/.tmux/conf/base.tmux"

source-file "$HOME/.tmux/conf/color.tmux"

source-file "$HOME/.tmux/conf/bind.tmux"

source-file "$HOME/.tmux/conf/plugin.tmux"


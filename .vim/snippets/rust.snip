snippet     mod-test
options     head
    #[cfg(test)]
    mod tests {
        use super::*;

        #[test]
        fn ${0:#:test_function_name}() {
        }
    }

snippet     test-logger
options     head
    extern crate env_logger;

    fn init() {
        let _ = env_logger::builder().is_test(true).try_init();
    }

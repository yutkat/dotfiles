# Keep NetworkManager from managing the sandboxed-Neovim egress bridge.
# Copy to /etc/NetworkManager/conf.d/ (non-NixOS hosts only, if NM is in use).
[keyfile]
unmanaged-devices=interface-name:nvbr0

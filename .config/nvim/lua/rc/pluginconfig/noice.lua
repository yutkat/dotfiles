require("noice").setup({
	popupmenu = {
		backend = "cmp", -- backend to use to show regular cmdline completions
	},
	notify = {
		enabled = false,
	},
	-- messages = {
	-- 	enabled = false,
	-- },
})

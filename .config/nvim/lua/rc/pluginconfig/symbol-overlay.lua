require("symbol-overlay").setup({
	keymap = {
		toggle = "<Leader>m",
		clear_all = "<Leader>M",
		next_highlight = nil,
		prev_highlight = nil,
		gen = nil,
		list = nil,
	},
})
-- require("telescope").load_extension("symbol_overlay")
